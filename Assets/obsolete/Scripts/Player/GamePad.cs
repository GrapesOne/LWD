using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GamePad : IClickableBotton {
/*
	private Player player;
	string NameOfController;
	[SerializeField] bool off = false, rl = false;

	void Start(){
		player = GameObject.FindGameObjectWithTag ("Player").GetComponent<Player> ();
		NameOfController = gameObject.name;
		NameOfController = NameOfController.Substring (0, NameOfController.IndexOf ('B'));
	}

	void OnMouseDown() {
		if (Check (4)) {
			player.Control [NameOfController] = true;
			if (off)
				Invoke ("Off", 0.1f);
		}
	}
	void OnMouseUp(){
		if (rl)
			player.Control [NameOfController] = false;
	}
	void Off(){
		player.Control [NameOfController] = false;
	}
*/

}
